# 2026-04-10 웹 최적화 진행 현황 + 인수인계

> 작성: Claude Opus 4.6 | 목적: 후임 AI/개발자가 이어서 작업할 수 있도록 현황 정리

---

## 오늘 완료한 것 (커밋 미완 — 푸시 필요)

### [A] 추천갤러리 → 랜딩 리다이렉트 제거 ✅
- **파일**: `public/index.js` — `handleFeaturedClick` 함수
- **수정**: `addLangToUrl(shareUrl)` 제거 → `openPageOverlay(shareUrl)` 직접 호출
- **이유**: URL `?lang=` 파라미터가 Google Translate 리다이렉트 유발 → 랜딩페이지로 이동
- **해결**: 독립페이지가 프로필처럼 `localStorage.getItem('appLanguage')`에서 직접 읽음

### [B] 언어/TTS 동기화 ✅
- **파일**: `public/index.html` — `executeLanguageSelection` 함수 (line 669-702)
- **수정**: 
  - `localStorage.removeItem('googtrans')` → `document.cookie` 삭제로 수정 (쿠키를 localStorage에서 지우려던 오류)
  - PWA 모드 분기 제거 → 모든 환경에서 reload 통일
  - Safari 한정 `triggerGoogleTranslate` 제거
- **원칙**: localStorage `appLanguage`가 유일한 언어 소스, 구글 번역 쿠키는 동기화

### [D] 저장 버튼 즉시 피드백 ✅
- **파일**: `public/index.js` — `handleSaveClick` 함수
- **수정**: `showToast("저장 완료!")` 제거 → 버튼 내 인라인 상태 변경 (카카오/토스 패턴)
  - 클릭 즉시: 스피너 표시 (animate-spin)
  - 성공: ✓ 체크마크 → 1.5초 후 원래 아이콘 복원
  - 실패: ✗ X마크 → 2초 후 원래 아이콘 복원
- **이유**: 기존 토스트가 서버 응답 후(~1초+) 표시 → "고장" 느낌

---

## 미완료 — 다음 세션에서 진행

### [E] iOS 웹(Safari+Chrome) 음성 자동재생 🟡
- **증상**: 아이폰 앱은 정상. 아이폰 웹에서만 첫 저장 후 TTS 작동
- **원인 조사 완료**: `synth.cancel()`이 iOS에서 세션 파괴 → 저장 클릭(제스처)이 세션 복구
- **현재 `synth.cancel()` 호출 14곳** 중 iOS 분기 적용은 2곳만
- **수정 방향**: 추가 코드 생성 아님. 기존 14곳의 `synth.cancel()` 중 iOS에서 불필요한 곳을 스킵하는 분기 추가
- **핵심 파일**: `public/index.js` — line 1378, 1702, 1742, 1780, 1784, 2640, 2792, 2847, 2904, 3049, 3906, 3939, 4185, 4249

### [F] 반응속도 / 페이지 전환 느림 🟡
- **증상**: 웹 전반적으로 느린 느낌
- **구조**: 모든 페이지가 index.html 안에 숨겨진 상태 (SPA) → `showPage()`로 CSS 전환
- **조사 필요**: 서버 응답 속도, 초기 로드 무게, CSS 전환 지연
- **기존 최적화**: `7f5d04f` 비디오 preload 차단, `b89983f` 불필요 파일 삭제

### [C] 위치정보 안 나타남 — 삼성 🟡
- **증상**: 삼성폰에서 위치정보 미표시
- **추정**: 권한 거부 가능성
- **조치**: 추가 관찰 후 결정

---

## 삼성 앱 현황 (TWA)

### TWA APK 빌드 성공 ✅
- **GitHub Actions**: `Build TWA APK` 워크플로우 — 자동화 완료
- **구조**: Gradle + android-browser-helper 2.5.0 (Chrome 엔진, WebView 아님)
- **서명**: 기존 keystore.jks (SHA-256: CD:4A:8A:7F...)
- **assetlinks.json**: `https://my-handyguide1.replit.app/.well-known/assetlinks.json` 배포 완료
- **테스트 필요**: 삼성 Galaxy A36에서 TWA APK 설치 → 버튼/화면전환/AI 호출 확인

### 기존 Expo WebView APK
- Android System WebView 렌더링 버그 (삼성 전 기종)
- 1개월간 모든 수정 시도 실패 → TWA로 전환

### iOS 앱
- 현재 최적화 상태 유지. 재빌드 불필요.

---

## 프로젝트 규칙 (CLAUDE.md 요약)
- 승인 없는 수정 금지
- 작동하는 코드 안 건드림
- `⚠️ 수정금지(승인필요)` 주석 코드 → 서면 승인 필요
- 커밋/푸시는 지시 시에만
- 모든 수정에 한국어 주석
- **Ralph Roof 방식**: 조사 → 코딩 → 검증(린트+빌드+simplify+review) → 통과 후 다음

---

## 메모리 파일 참조
- `~/.claude/projects/.../memory/MEMORY.md` — 사용자 프로필, 피드백, 프로젝트 현황
- `~/.claude/plans/vivid-tumbling-forest.md` — 현재 작업 플랜

---

## 배포 순서 (확정 후)
1. 웹 수정 [A][B][D] 커밋 + 푸시
2. Replit 재배포 (git pull + 재시작)
3. 전 플랫폼 자동 반영 (TWA/iOS앱/브라우저)
4. 삼성 TWA APK 테스트
5. 최종 확정 후 1회만: Google Play AAB + App Store IPA 빌드/업로드
