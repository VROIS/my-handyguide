# 2026-04-11 웹 최적화 진행 현황 + 인수인계

> 작성: Claude Opus 4.6 | 최종 갱신: 2026-04-11

---

## 완료 — 커밋/푸시 대기

### [A] 추천갤러리 → 랜딩 리다이렉트 제거 ✅ (04-10)
- **파일**: `public/index.js` — `handleFeaturedClick`
- **수정**: `addLangToUrl(shareUrl)` 제거 → `openPageOverlay(shareUrl)` 직접 호출

### [B] 언어/TTS 동기화 ✅ (04-10)
- **파일**: `public/index.html` — `executeLanguageSelection` (line 669-702)
- **수정**: googtrans 쿠키 삭제 방식 수정, PWA 분기 제거, Safari 한정 로직 제거

### [D] 저장 버튼 즉시 피드백 ✅ (04-10)
- **파일**: `public/index.js` — `handleSaveClick`
- **수정**: 토스트 → 버튼 인라인 상태 변경 (스피너→체크→복원)

### [E] iOS 웹 TTS 수정 ✅ (04-11)
- **파일**: `public/index.js`
- **수정**:
  - `synth.cancel()` 중복 호출 **11곳 삭제** — `resetSpeechState()` 내부에서 이미 cancel 호출하므로 불필요
  - 핵심 **3곳 iOS 스킵** 추가 — `stopAudio()`, `resetSpeechState()` x2
  - 패턴: `if (detectPlatform() !== 'ios') { synth.cancel(); }` (기존 03-23 방식)
- **원인**: 앱 개발 과정(03-17 Chrome 버그 대응)에서 추가된 cancel()이 웹에도 적용 → iOS 오디오 세션 파괴
- **검증**: simplify 리뷰 완료, 지적사항 없음

### [F] 웹 반응속도 최적화 — Phase 1+2 ✅ (04-11)
- **파일**: `public/index.html` (CSS/HTML 추가만, JS 변경 없음)
- **수정**:
  - **F-1** `content-visibility: hidden` — 비활성 5페이지 렌더링 비용 제거 (5-8x 개선)
  - **F-3** 터치 피드백 — `button:active` scale(0.97)+opacity(0.7) (기존 .interactive-btn 제외)
  - **F-4** `will-change: opacity` — 활성 페이지만 GPU 가속 (숨긴 페이지 메모리 낭비 방지)
  - **F-7** 폰트 비블로킹 — `media="print" onload` 패턴 (FCP -300ms)
  - **F-5** Tailwind defer — ❌ 롤백 (FOUC 위험, simplify에서 발견)
- **검증**: simplify 리뷰 완료, 2건 수정 반영 (CSS 충돌 방지, GPU 메모리 범위 조정)

---

## 미완료

### [F] Phase 3 — 대량 데이터 최적화 🟡
- 아카이브 가상 스크롤/페이지네이션 (500개+ 아이템 프리즈)
- 아카이브 이미지 lazy loading
- **JS 변경 필요** → 별도 승인 후 진행

### [C] 위치정보 안 나타남 — 삼성 🟡
- 삼성폰에서 권한 확인 필요 → 사용자 관찰 후 결정

---

## 삼성 앱 현황 (TWA)

### TWA APK ✅
- GitHub Actions `Build TWA APK` 자동화 완료
- Gradle + android-browser-helper 2.5.0 (Chrome 엔진)
- assetlinks.json 배포 완료
- **APK 재빌드 불필요** — TWA는 웹을 직접 로딩하므로 웹 재배포만으로 자동 반영

### iOS 앱
- 현재 상태 유지. 재빌드 불필요.

---

## 배포 순서

1. **커밋 + 푸시** — [A][B][D][E][F] 전체 (사용자 지시 시)
2. **Replit 재배포** — git pull + 재시작 (사용자)
3. **자동 반영 확인** — 웹 + TWA(삼성) + iOS앱 (사용자 테스트)
4. **APK 재빌드 불필요** — 네이티브 설정 변경 없음
5. **최종 확정 후** — Google Play AAB + App Store IPA (1회만)

---

## 프로젝트 규칙 (CLAUDE.md 요약)
- 승인 없는 수정 금지
- 작동하는 코드 안 건드림
- `⚠️ 수정금지(승인필요)` 주석 코드 → 서면 승인 필요
- 커밋/푸시는 지시 시에만
- 모든 수정에 한국어 주석
- **Ralph Roof 방식**: 조사 → 코딩 → 검증(빌드+simplify+review) → 통과 후 다음
